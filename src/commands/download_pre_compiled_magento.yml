description: >
  Download pre-compiled Magento from S3

parameters:
  version:
    type: string
    default: ""
steps:
  - run:
      name: Download Magento from ebizmartsbackup S3
      environment:
        PARAM_VERSION: << parameters.version >>
      command: <<include(src/scripts/ebiz_mage_backup.sh)>>